# Dependencies
node_modules/
.pnpm-store/

# Build output
.next/
out/
dist/
build/

# Environment — only .env.local.example is committed
.env
.env.local
.env.production
.env.development
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json

# Testing
coverage/
.nyc_output/
playwright-report/
playwright/.cache/
test-results/

# Next.js
next-env.d.ts

# Vercel
.vercel/

# Drizzle
drizzle/meta/_journal.json.backup

# TypeScript
*.tsbuildinfo

# Misc
.turbo/
.cache/

# Metrics dashboard live output — generate.mjs writes real Stripe/DB
# figures here on every run. Never commit it; -sample.html is the demo.
scripts/metrics-dashboard/metrics-dashboard.html

# Claude plans are working notes, not source of truth — but we keep them
# so don't ignore .claude/plans/
.claude/scheduled_tasks.lock
.claude/scheduled_tasks.json

# Security audit exports — external artifacts, not source
Tablekit-Security-Audit.docx
